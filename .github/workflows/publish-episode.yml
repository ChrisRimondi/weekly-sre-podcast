name: Publish podcast episode

on:
  schedule:
    - cron: "0 11 * * 6"
  workflow_dispatch:
    inputs:
      note_path:
        description: "Optional existing note path, for example notes/weekly-sre-2026-07-04.md"
        required: false
        type: string
      episode_date:
        description: "Optional episode date to generate, in YYYY-MM-DD format"
        required: false
        type: string
  push:
    paths:
      - "notes/weekly-sre-*.md"

permissions:
  contents: write

concurrency:
  group: publish-podcast-episode
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    env:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      OPENAI_TEXT_MODEL: ${{ vars.OPENAI_TEXT_MODEL || 'gpt-4.1' }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Install ffmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Configure git author
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Select episode note
        id: note
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ -n "${{ inputs.note_path }}" ]; then
            note_path="${{ inputs.note_path }}"
          elif [ "${{ github.event_name }}" = "push" ]; then
            note_path="$(git diff-tree --no-commit-id --name-only -r "${{ github.sha }}" | grep '^notes/weekly-sre-.*\.md$' | sort | tail -1)"
          elif [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ -n "${{ inputs.episode_date }}" ]; then
            note_path="$(npm run --silent generate:episode -- "${{ inputs.episode_date }}" | tail -1)"
          else
            note_path="$(npm run --silent generate:episode | tail -1)"
          fi

          if [ -z "$note_path" ]; then
            echo "No weekly SRE note selected or generated."
            exit 1
          fi

          if [ ! -f "$note_path" ]; then
            echo "Note not found: $note_path"
            exit 1
          fi

          echo "note_path=$note_path" >> "$GITHUB_OUTPUT"

      - name: Publish episode
        run: npm run publish:episode -- "${{ steps.note.outputs.note_path }}"
